---

tasks:
  rockylinux8:
    shell_commands:
      - rm -rf $HOME/bazeltest
      - mkdir $HOME/bazeltest
    build_flags:
      - "--config=ci-linux"
    build_targets:
      - "//src/test/shell/bazel:ggvdp_test"
    test_flags:
      - "--config=ci-linux"
    test_targets:
      - "//src/test/shell/bazel:ggvdp_test"
