[
    {
        "node": "docker",
        "shard": "1",
        "parameters": {
            "targets": [],
            "tests": [
                "filter(ubuntu, //src/test/docker/...)"
            ]
        }
    },
    {
        "node": "docker",
        "shard": "2",
        "parameters": {
            "targets": [],
            "tests": [
                "//src/test/docker/... - filter(ubuntu, //src/test/docker/...)"
            ]
        }
    }
]
