filegroup(
    name = "zipper",
    # On Linux/macOS: zipper
    # On Windows: zipper.exe
    # Use glob() so it matches whichever file is present.
    srcs = glob(["zipper*"]),
    visibility = ["//visibility:public"],
)
